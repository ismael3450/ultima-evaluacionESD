mod graph_core;
mod data_loader;
mod ui;
mod algorithms;

fn main() {
    println!("--- Sistema de Monitoreo de Red Inicializado ---");

    // Aquí se conectará el flujo principal más adelante:
    // let datos = data_loader::inicializar_datos();
    // let grafo = graph_core::construir_grafo(datos);

    // Así los llamarán en el main:
    // algorithms::bfs::ejecutar_bfs(&grafo);
    // algorithms::dfs::ejecutar_dfs(&grafo);
}